services:
  # Backend Web Service
  - type: web
    name: nb-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npx prisma generate
    startCommand: npx prisma db push --accept-data-loss && npm start
    envVars:
      - key: DATABASE_URL
        value: "mongodb+srv://fixlyplaykers_db_user:fixlyplayker@cluster0.fbleyeo.mongodb.net/nineteen06?retryWrites=true&w=majority"
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production

  # Frontend Static Site
  - type: static
    name: nb-frontend
    plan: free
    rootDir: frontend
    buildCommand: npm install && npm run build
    publishDir: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://nb-backend-2yow.onrender.com
